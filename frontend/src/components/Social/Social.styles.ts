import styled from 'styled-components';

export const SocialRoot = styled.ul`
   display: flex;
   justify-content: center;
   gap: 20px;
   margin-bottom: 0.5rem;

   button {
      background: transparent;
      outline: none;
      border: none;
   }
`;
